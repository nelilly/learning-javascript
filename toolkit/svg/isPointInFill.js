/* Description: This function checks if a point is inside the fill area of a given SVG path. */
/* Usage: circle.isPointInFill(pointObj); */
/* Parameters: pointObj - An object containing x and y coordinates of the point to be checked. */
/* Returns: true if the point is inside the fill area, false otherwise. */
/* Example: circle.isPointInFill({ x: 50, y: 50 }); */

circle.isPointInFill(pointObj); 
