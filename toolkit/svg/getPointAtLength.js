/**
 * Calculates the point at a specified distance along an SVG path.
 *
 * @param {number} distance - The distance along the path at which to calculate the point.
 * @returns {{x: number, y: number}} An object containing the x and y coordinates of the point at the specified distance.
 */
getPointAtLength(distance)
